# Competition Rules

The full IGVC qualification and function-testing rule reference is currently preserved at the repo root:

- [../../rules.md](../../rules.md)

Keep this page as the stable docs entry point for IGVC rules. If the root file is later relocated, move the full contents here and leave a root stub pointing back to this page.

## Implementation Notes

- The scenario implementation map lives in [Overview](overview.md).
- Rule text should stay separate from source-code implementation notes.
- When a rule becomes executable as a mini scenario, link it from [Overview](overview.md) to the matching file under `app/3d/igvc/mini/`.
