/* tslint:disable */
/* eslint-disable */

/**
 * Pack standard xyz+intensity PointCloud2 (16 bytes/point).
 * Returns little-endian packed bytes. Width = data.len() / 16.
 */
export function pack_pointcloud2(buffer: Float32Array, range: number, az_start: number, az_end: number, az_step: number, el_start: number, el_end: number, el_step: number, encoding: string): Uint8Array;

/**
 * Pack semantic PointCloud2 (28 bytes/point).
 * Returns little-endian packed bytes. Width = data.len() / 28.
 */
export function pack_semantic_pointcloud2(buffer: Float32Array, range: number, az_start: number, az_end: number, az_step: number, el_start: number, el_end: number, el_step: number, encoding: string): Uint8Array;

export type InitInput = RequestInfo | URL | Response | BufferSource | WebAssembly.Module;

export interface InitOutput {
    readonly memory: WebAssembly.Memory;
    readonly pack_pointcloud2: (a: number, b: number, c: number, d: number, e: number, f: number, g: number, h: number, i: number, j: number, k: number) => [number, number];
    readonly pack_semantic_pointcloud2: (a: number, b: number, c: number, d: number, e: number, f: number, g: number, h: number, i: number, j: number, k: number) => [number, number];
    readonly __wbindgen_externrefs: WebAssembly.Table;
    readonly __wbindgen_malloc: (a: number, b: number) => number;
    readonly __wbindgen_realloc: (a: number, b: number, c: number, d: number) => number;
    readonly __wbindgen_free: (a: number, b: number, c: number) => void;
    readonly __wbindgen_start: () => void;
}

export type SyncInitInput = BufferSource | WebAssembly.Module;

/**
 * Instantiates the given `module`, which can either be bytes or
 * a precompiled `WebAssembly.Module`.
 *
 * @param {{ module: SyncInitInput }} module - Passing `SyncInitInput` directly is deprecated.
 *
 * @returns {InitOutput}
 */
export function initSync(module: { module: SyncInitInput } | SyncInitInput): InitOutput;

/**
 * If `module_or_path` is {RequestInfo} or {URL}, makes a request and
 * for everything else, calls `WebAssembly.instantiate` directly.
 *
 * @param {{ module_or_path: InitInput | Promise<InitInput> }} module_or_path - Passing `InitInput` directly is deprecated.
 *
 * @returns {Promise<InitOutput>}
 */
export default function __wbg_init (module_or_path?: { module_or_path: InitInput | Promise<InitInput> } | InitInput | Promise<InitInput>): Promise<InitOutput>;
