
import { SparkRenderer, SplatMesh } from "@sparkjsdev/spark";
import { Data } from "../../data/Data";


/**
 * BakedRender is responsible for rendering the baked gaussian splats using SparkRenderer.
 * It takes the baked data from the AI rendering pipeline and renders it in the scene.
 */
class BakedRender {
    /**
     * 
     * @param {Data} data 
     */
    constructor(data) {

    }


}