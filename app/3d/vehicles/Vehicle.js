

class Vehicle {
    /**
     * 
     * @param {String} id Id of the vehicle
     * @param {String} model Model of the vehicle (.glb, .gltf, etc.)
     */
    constructor(id, model) {
        this.id = id;
        this.model = model;
    }
}